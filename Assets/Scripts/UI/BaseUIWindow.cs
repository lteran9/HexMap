using UnityEngine;
using UnityEngine.UIElements;

namespace HexMap.UI
{
   public class BaseUIWindow : MonoBehaviour
   {
      // Root of all functionality
      protected UIDocument _uiDocument = default;
   }
}